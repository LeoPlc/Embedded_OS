#include <stdio.h>
using namespace std;

/*
ALGORITHME NAÏF

DFT
    INPUT: Array x
    OUTPUT: Array x (Size of x, initialized with zeros)

    N = length(x)
    for k in 0...N-1 do:
        for n in 0...N-1
            X[k] = X[k] + X[n] * exp(-2*j*pi/N)

*/


int main(){
    return 0;
}